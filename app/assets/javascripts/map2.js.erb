$(document).ready(function() {
  var map = WE.map('earth_div', {
            center: [51.505, -0.09],
            zoom: 4,
            dragging: true,
            scrollWheelZoom: true,
            proxyHost: 'http://srtm.webglearth.com/cgi-bin/corsproxy.fcgi?url='
          });

          //Add baselayer
          WE.tileLayer('http://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{
            attribution: '© OpenStreetMap contributors'
          }).addTo(map);

          $(item).find('test_div').text("hello testing");



          $(".satellites-table > tbody > tr").each(function(index, item)
          {
            // var lat = $(item).find(".sat-latitude").text();
            // var lon = $(item).find(".sat-longitude").text();
            // var name = $(item).find(".satellite-name").text();
            // var marker = WE.marker([parseInt(lat, 10), parseInt(lon, 10)]).addTo(map);
            // marker.bindPopup(name);



            $(item).find('test_div').text("i_rann_once");

          });

          // var marker = WE.marker([51.5, -0.1]).addTo(map);
          // marker.bindPopup('<b>Hello world!</b>');

});
